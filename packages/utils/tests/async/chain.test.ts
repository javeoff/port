import { chain } from '../../src/async/chain';

describe('chain', () => {
  console.log(12);
  // const testFn = (str: string) => str;

  // const fn = chain().then(testFn, '123');

  // expect(fn()).toBe('123');
});
